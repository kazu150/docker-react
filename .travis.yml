language: generic 
sudo: required
services:
    - docker

before_install:
    - docker build -t kazu1500/docker-react  -f Dockerfile.dev .


script:
    - docker run -e CI=true kazu1500/docker-react npm run test -- --coverage